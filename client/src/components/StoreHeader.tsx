/** Ribbon Modernism navigation: crisp, editorial storefront routes now connect real account, category, and cart journeys. */
import { Menu, Search, ShoppingBag, SlidersHorizontal, UserRound, X } from "lucide-react";
import { useState } from "react";
import { useCart } from "@/contexts/CartContext";
import { useAuth } from "@/contexts/AuthContext";
import { BrandMark } from "./BrandMark";
export function StoreHeader() { const [mobileOpen, setMobileOpen] = useState(false); const { count } = useCart(); const { user } = useAuth(); const links = [["Shop", "/shop"], ["Categories", "/categories"], ["The Edit", "/#the-edit"], ["Our point of view", "/#story"]] as const; return <><div className="utility-ribbon">Free delivery on edits over ৳2,000 <span>•</span> Bangladesh beauty, considered</div><header className="store-header"><button className="mobile-menu" onClick={() => setMobileOpen(true)} aria-label="Open navigation"><Menu size={21} /></button><BrandMark /><nav className="desktop-nav" aria-label="Main navigation">{links.map(([label, href]) => <a key={label} href={href}>{label}</a>)}</nav><div className="header-tools"><a href="/shop" aria-label="Search products"><Search size={20} /></a><a href="/account" aria-label="Open customer account"><UserRound size={19} /><span className="account-label">{user ? user.name.split(" ")[0] : "Account"}</span></a><a className="studio-link" href="/admin"><SlidersHorizontal size={17} /><span>Studio</span></a><a className="bag-button" href="/cart" aria-label={`Open bag with ${count} items`}><ShoppingBag size={20} /><span>{count}</span></a></div></header>{mobileOpen && <div className="mobile-nav-wrap" role="dialog" aria-modal="true" aria-label="Mobile navigation"><div className="mobile-nav"><div className="mobile-nav-top"><BrandMark /><button onClick={() => setMobileOpen(false)} aria-label="Close navigation"><X size={22} /></button></div>{links.map(([label, href], index) => <a key={label} href={href} onClick={() => setMobileOpen(false)}><span>0{index + 1}</span>{label}</a>)}<a href="/account" onClick={() => setMobileOpen(false)}><span>05</span>Account</a><a href="/cart" onClick={() => setMobileOpen(false)}><span>06</span>Shopping bag</a><a href="/admin" onClick={() => setMobileOpen(false)}><span>07</span>Studio dashboard</a></div></div>}</>; }
