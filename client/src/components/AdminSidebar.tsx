/** Ribbon Modernism Studio navigation: a factual operational index for the v2 Cloudflare database, not a decorative placeholder rail. */
import { Boxes, ChartNoAxesCombined, FolderOpen, LayoutPanelTop, LogOut, PackagePlus, Settings, ShoppingBag, Tags } from "lucide-react";
import { BrandMark } from "./BrandMark";
export type AdminView = "overview" | "catalogue" | "categories" | "inventory" | "media" | "orders" | "settings";
const navItems: { id: AdminView; label: string; icon: typeof ChartNoAxesCombined }[] = [{ id: "overview", label: "Overview", icon: ChartNoAxesCombined }, { id: "catalogue", label: "Product library", icon: PackagePlus }, { id: "categories", label: "Categories", icon: Tags }, { id: "inventory", label: "Inventory", icon: Boxes }, { id: "media", label: "Media desk", icon: FolderOpen }, { id: "orders", label: "Orders & tracking", icon: ShoppingBag }, { id: "settings", label: "Connections", icon: Settings }];
export function AdminSidebar({ active, onChange, onLogout }: { active: AdminView; onChange: (view: AdminView) => void; onLogout: () => void }) { return <aside className="admin-sidebar"><div className="admin-brand"><BrandMark compact /><span>STUDIO</span></div><div className="admin-sidebar-label">Workspace</div><nav>{navItems.map((item) => { const Icon = item.icon; return <button key={item.id} className={active === item.id ? "active" : ""} onClick={() => onChange(item.id)}><Icon size={18} />{item.label}</button>; })}</nav><div className="admin-sidebar-foot"><a href="/"><LayoutPanelTop size={17} />View shop</a><button onClick={onLogout}><LogOut size={17} />Sign out</button></div></aside>; }
